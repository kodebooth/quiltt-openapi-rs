/*
 * REST API
 *
 * # Overview The REST API allows you to handle authentication, manage Profiles and perform certain server-to-server operations in your Quiltt Environment.  To download and interact with detailed Profile data, including Connections, Accounts, Transactions, and Statements, use the [GraphQL API](https://www.quiltt.dev/api-reference/graphql).  See the [API Overview](https://www.quiltt.dev/api) to learn more about Quiltt's Hybrid API model.  # API Host  Auth-related requests - like issuing session tokens or introspecting credentials - use the Auth server:  <pre><code>https://auth.quiltt.io</code></pre>  All other API requests use the core API server:  <pre><code>https://api.quiltt.io</code></pre>  # Authentication  All requests, except for certain Auth endpoints, require a valid API Key secret to be passed in as a bearer token in the Authorization header:  <pre><code>Authorization: Bearer {API_KEY_SECRET}</code></pre>  You can access your API Keys in the [Quiltt Dashboard](https://dashboard.quiltt.dev/api-keys).  # Errors Unsuccessful requests will return a JSON object to help troubleshoot what went wrong.  ### StandardError A generic error with troubleshooting instructions.  Returned with all `4xx` or `5xx` HTTP status codes, except `422` errors on some REST endpoints. <SchemaDefinition schemaRef=\"#/components/schemas/RestError\" />  ### ValidationErrors One or more correctable errors encountered when attempting to create or update a record.  Returned with a `422` HTTP status code. <SchemaDefinition schemaRef=\"#/components/schemas/ValidationErrors\" /> 
 *
 * The version of the OpenAPI document: 2026-04-01
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ConnectionImportPlaidRequest : Options to import a Connection from Plaid.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ConnectionImportPlaidRequest {
    /// The token to import from Plaid.
    #[serde(rename = "accessToken")]
    pub access_token: String,
    /// The mode of the import operation. Use `BACKGROUND` when importing batches of Connections, or when you don't need immediate access to synced data.  - `REALTIME`: Syncing process will be kicked off immediately. - `BACKGROUND`: Syncing will occur shortly after import, with a slight delay.
    #[serde(rename = "mode", skip_serializing_if = "Option::is_none")]
    pub mode: Option<Mode>,
    /// Specifies whether this Connection is managed by an external system.  When a Connection is imported as externally-managed, Quiltt will sync it, but disconnecting it will not revoke access from the upstream provider.
    #[serde(rename = "externallyManaged", skip_serializing_if = "Option::is_none")]
    pub externally_managed: Option<bool>,
    /// Whether to migrate the webhook registered with Plaid to be consumed by Quiltt.  When set to `false`, Quiltt will preserve the existing webhook registered with Plaid and the Connection will sync less frequently.
    #[serde(rename = "migrateWebhook", skip_serializing_if = "Option::is_none")]
    pub migrate_webhook: Option<bool>,
    /// The timestamp when the Plaid Item was created, in [ISO 8601](https://wikipedia.org/wiki/ISO_8601) format.
    #[serde(rename = "createdAt", skip_serializing_if = "Option::is_none")]
    pub created_at: Option<chrono::DateTime<chrono::FixedOffset>>,
    /// The authorized Plaid products to sync on the Connection.  These products will be added to the products Quiltt can detect automatically. Note that `balance`, `investments` and `liabilities` are always detected automatically.
    #[serde(rename = "products", skip_serializing_if = "Option::is_none")]
    pub products: Option<Vec<Products>>,
    /// The Plaid Institution ID, to be supplied only if directed by Quiltt
    #[serde(rename = "institutionId", skip_serializing_if = "Option::is_none")]
    pub institution_id: Option<String>,
}

impl ConnectionImportPlaidRequest {
    /// Options to import a Connection from Plaid.
    pub fn new(access_token: String) -> ConnectionImportPlaidRequest {
        ConnectionImportPlaidRequest {
            access_token,
            mode: None,
            externally_managed: None,
            migrate_webhook: None,
            created_at: None,
            products: None,
            institution_id: None,
        }
    }
}
/// The mode of the import operation. Use `BACKGROUND` when importing batches of Connections, or when you don't need immediate access to synced data.  - `REALTIME`: Syncing process will be kicked off immediately. - `BACKGROUND`: Syncing will occur shortly after import, with a slight delay.
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum Mode {
    #[serde(rename = "REALTIME")]
    Realtime,
    #[serde(rename = "BACKGROUND")]
    Background,
}

impl Default for Mode {
    fn default() -> Mode {
        Self::Realtime
    }
}
/// The authorized Plaid products to sync on the Connection.  These products will be added to the products Quiltt can detect automatically. Note that `balance`, `investments` and `liabilities` are always detected automatically.
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum Products {
    #[serde(rename = "auth")]
    Auth,
    #[serde(rename = "identity")]
    Identity,
    #[serde(rename = "transactions")]
    Transactions,
}

impl Default for Products {
    fn default() -> Products {
        Self::Auth
    }
}

